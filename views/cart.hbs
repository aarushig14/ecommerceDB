<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <title>Title</title>
    <script src="jquery-3.2.0.js"></script>
    <script src="client.js"></script>
</head>
<body>
    <div>
        {{#each cart as |item|}}
            <div>
                <img src="{{item.imgsrc}}">
                <h6>{{item.name}}</h6>
                <h6>{{item.qty}}</h6>
                <h6>Price : {{item.price}} x {{item.qty}}</h6>
                <h6>Total Price: {{item.totalprice}}</h6>
            </div>
        {{/each}}
    </div>
</body>
</html>